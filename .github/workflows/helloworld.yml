name : "Hello world"

on:
    push:
        branches:
            -master

jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: "Print Hello world"
              run: echo "hello world"